pre-commit:
  parallel: true
  commands:
    format:
      glob: "*.rs"
      run: cargo fmt -- --check {staged_files}
    clippy:
      glob: "*.rs"
      run: cargo clippy --all-targets --all-features -- -D warnings

pre-push:
  commands:
    check:
      run: just check
