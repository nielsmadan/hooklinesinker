Pick next feature
Can parallelize? Send off an agent to do it in a worktree.
Run /plan
Big feature? Run /review-plan --multi
Implement
QA
If FE, check that design matches the old app as much as possible.
Run /code-review --multi or /code-review --quick for small features.
Fix everything.
Squash commits.
If worktree re-integrate to main.
