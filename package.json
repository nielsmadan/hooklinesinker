{
  "name": "hooklinesinker-adapters",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "type-check": "tsc --noEmit",
    "lint": "oxlint -A all -D typescript/no-explicit-any adapters/",
    "check-all": "npm run type-check && npm run lint"
  },
  "devDependencies": {
    "@types/node": "24.13.4",
    "oxlint": "1.82.0",
    "typescript": "7.0.2"
  }
}
